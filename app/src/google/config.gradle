ext {
    // AdMob配置
    admob = [applicationId: "ca-app-pub-7869168547621070~1208617379", // 测试应用ID，请替换为实际的AdMob应用ID
             adUnitIds    : [banner      : "",        // 横幅广告测试ID
                             interstitial: "ca-app-pub-7869168547621070/1657953759", // 插页广告测试ID
                             splash      : "ca-app-pub-7869168547621070/5760016145",       // 开屏广告测试ID
                             native      : "ca-app-pub-7869168547621070/2779463737",        // 原生广告测试ID
                             full_native : "ca-app-pub-7869168547621070/2779463737",        // 全屏原生广告测试ID
                             rewarded    : ""         // 激励广告测试ID
             ]]

    // Google Ad Manager 配置（按业务替换为正式 ID）
    gam = [adUnitIds: [banner      : "",
                       interstitial: "/23312177113/UniversalRemote_IV",
                       splash      : "/23312177113/UniversalRemote_SP",
                       native      : "/23312177113/UniversalRemote_NA",
                       full_native : "/23312177113/UniversalRemote_NA",
                       rewarded    : ""
    ]]

    // Pangle配置
    pangle = [applicationId: "8849294", // Pangle测试应用ID
              adUnitIds    : [splash      : "890164175",        // 开屏广告测试ID（竖屏）
                              banner      : "",        // 横幅广告测试ID（320x50）
                              interstitial: "983317221",       // 插页广告测试ID（竖屏）
                              native      : "983317351",        // 原生广告测试ID
                              full_native : "983317351",        // 全屏原生广告测试ID
                              rewarded    : ""         // 激励视频测试ID（竖屏）
              ]]

    // TopOn配置
    topon = [applicationId: "h1heqsg0d8jt5a",           // TopOn 应用 ID（需替换为实际值）
             appKey       : "a256dd7b849391f288ee9ece42a7e5687",          // TopOn 应用密钥（需替换为实际值）
             adUnitIds    : [interstitial: "n1heqsg0fl3kl7",  // 插页广告位 ID（需替换为实际值）
                             rewarded    : "",  // 激励广告位 ID
                             native      : "n1heqsg0g285gc",  // 原生广告位 ID（需替换为实际值）
                             splash      : "n1heqsg0h13l2c",  // 开屏广告位 ID（需替换为实际值）
                             full_native : "n1heqsg0g285gc",  // 全屏原生广告位 ID（需替换为实际值）
                             banner      : ""]  // 横幅广告位 ID（需替换为实际值）
    ]

    // MAX (AppLovin) 配置
    max = [sdkKey   : "",  // Demo SDK Key（测试用）
           adUnitIds: [splash      : "",      // 开屏广告位 ID
                       banner      : "",      // 横幅广告位 ID
                       interstitial: "",// 插页广告位 ID
                       native      : "",      // 原生广告位 ID
                       fullNative  : "", // 全屏原生广告位 ID
                       rewarded    : ""]    // 激励广告位 ID
    ]

    // 应用版本配置
    app = [applicationId: "com.universal.remote.tool",
           compileSdk   : 36,
           minSdk       : 26,
           targetSdk    : 36,
           versionCode  : 1,
           versionName  : "1.0.0"]

    // 统计归因配置
    analytics = [adjustAppToken: "hopjs50gmuww",           // Adjust App Token
                 thinkingDataAppId: "871c0ebde6be4329b9b74acb3f08ba36",   // 数数 SDK APP ID
                 thinkingDataServerUrl: "https://ds.devs343.com",     // 数数上报域名
                 defaultUserChannel: "natural"]                           // 默认用户渠道，internal默认paid
}
