<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/content_container"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:background="@drawable/bg_native_ad_card"
        android:paddingHorizontal="12dp"
        android:paddingVertical="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- App icon -->
        <androidx.cardview.widget.CardView
            android:id="@+id/iconCv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:cardCornerRadius="8dp"
            app:cardElevation="0dp"
            app:layout_constraintEnd_toStartOf="@id/contentLL"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:id="@+id/iv_ad_icon"
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:scaleType="centerCrop"
                android:src="@android:drawable/ic_menu_info_details" />
        </androidx.cardview.widget.CardView>

        <!-- Ad content area -->
        <LinearLayout
            android:id="@+id/contentLL"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/btn_ad_cta"
            app:layout_constraintStart_toEndOf="@id/iconCv"
            app:layout_constraintTop_toTopOf="parent">

            <!-- Ad title -->
            <TextView
                android:id="@+id/tv_ad_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="sans-serif-light"
                android:maxLines="1"
                android:text="@string/ad_test_pangle"
                android:textColor="#333333"
                android:textSize="14sp" />

            <!-- Ad description (optional) -->
            <TextView
                android:id="@+id/tv_ad_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="5dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/ad_test_pangle_desc"
                android:textColor="#666666"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- CTA button -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/btn_ad_cta"
            android:layout_width="wrap_content"
            android:layout_height="28dp"
            android:background="@drawable/bg_ad_cta_button"
            android:fontFamily="sans-serif-light"
            android:gravity="center"
            android:paddingHorizontal="12dp"
            android:singleLine="true"
            android:text="@string/ad_install"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/contentLL"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Top ad logo container -->
        <FrameLayout
            app:layout_constraintBottom_toBottomOf="@id/iconCv"
            app:layout_constraintStart_toStartOf="@id/iconCv"
            app:layout_constraintEnd_toEndOf="@id/iconCv"
            android:id="@+id/fl_ad_logo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone" />

    </androidx.constraintlayout.widget.ConstraintLayout>
    <com.android.common.bill.ui.view.AdLabelView
        android:id="@+id/tv_ad_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        />
</androidx.constraintlayout.widget.ConstraintLayout>
